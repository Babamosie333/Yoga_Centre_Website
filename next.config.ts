import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
      {
        protocol: 'http',
        hostname: '**',
      },
    ],
  },
  // Netlify handles Next.js perfectly without these specific flags usually
  // but keeping remotePatterns is good.
};

export default nextConfig;
